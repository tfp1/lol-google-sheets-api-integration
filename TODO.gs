// Get a single match
// Parse what records we want from the match JSON


// Seed all existing matches
// Get latest matches (do we have to get count of matches, then determine how many to get first?)
